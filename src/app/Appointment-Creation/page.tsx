import AppointmentForm from '@/components/AppointmentForm'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <AppointmentForm/>
    </div>
  )
}

export default page